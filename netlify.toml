[build]
  command = "pnpm --filter @workspace/proquoment-website... run build"
  publish = "artifacts/proquoment-website/dist"

[build.environment]
  BASE_PATH = "/"
  PORT = "5173"
  NODE_ENV = "production"
